import React from 'react';
import { DEVICE_TYPES_DISPLAY_PROPERTIES } from './Globals'

function Breadcrumbs(props) {
    return (
        <div className={`Menu-container-flex ${props['theme']}`}>BREADCRUMBS</div>
    );
}

export default Breadcrumbs;